<!-- Offcanvas Chat Container -->
<div class="offcanvas offcanvas-end" tabindex="-1" id="chatOffcanvas" aria-labelledby="chatOffcanvasLabel">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title" id="chatOffcanvasLabel">Chat</h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body d-flex flex-column">
      <!-- Chat messages -->
      <div id="chat-messages" class="flex-grow-1 overflow-auto bg-light text-dark">
        <!-- Loop through chatMessages array to display each message -->
        <div *ngFor="let chatMessage of chatMessages" [ngClass]="{'bg-white': chatMessage.type === 'ai', '': chatMessage.type === 'user'}" class="d-flex p-3">
          <!-- User message -->
          <img *ngIf="chatMessage.type === 'user'" src="https://dummyimage.com/256x256/363536/ffffff&text=U" class="rounded-circle me-2" style="width: 30px; height: 30px" />
          <img *ngIf="chatMessage.type === 'ai'" src="https://dummyimage.com/256x256/354ea1/ffffff&text=G" class="rounded-circle me-2" style="width: 30px; height: 30px" />
          
          <div class="d-flex flex-column w-100">
            <p class="mb-2 small">{{ chatMessage.message }}</p>
            <!-- Thumbs up, thumbs down, clipboard buttons -->
            <div *ngIf="chatMessage.type === 'ai'" class="d-flex align-items-center gap-2">
              <button class="btn btn-link text-secondary p-0">
                <i class="bi bi-hand-thumbs-up"></i>
              </button>
              <button class="btn btn-link text-secondary p-0">
                <i class="bi bi-hand-thumbs-down"></i>
              </button>
              <button class="btn btn-link text-secondary p-0">
                <i class="bi bi-clipboard"></i>
              </button>
            </div>
          </div>
        </div>
      </div>
  
      <!-- Prompt message input -->
      <form class="d-flex align-items-center bg-light border-top p-2" (submit)="sendMessage()">
        <button class="btn btn-link text-dark" type="button">
          <i class="bi bi-plus-lg"></i>
        </button>
        <textarea id="chat-input" class="form-control mx-2" [(ngModel)]="message" name="message" rows="1" placeholder="Enter your prompt"></textarea>
        <button class="btn btn-primary" type="submit">Send</button>
      </form>
    </div>
  </div>
  
  <!-- Chat toggle button -->
  <button class="btn btn-primary position-fixed bottom-0 end-0 m-3" type="button" data-bs-toggle="offcanvas" data-bs-target="#chatOffcanvas" aria-controls="chatOffcanvas">
    <i class="bi bi-chat-dots"></i> Chat
  </button>
  